# 原始文本参考 + 完整提示词预览

**Status:** ready-for-agent

**Blocked by:** 01

**构建内容：** 编辑器中展示当前 agent preset 的原始默认身份声明和人设描述文本（只读），供用户参考覆盖前的内容。提供"预览完整提示词"功能，展示当前配置下所有层合并后的最终系统提示词文本，让用户验证覆盖效果。

**验收标准：**

- [ ] 设置面板编辑器中展示当前 preset 的原始身份声明文本（只读，标注"默认值"）
- [ ] 设置面板编辑器中展示当前 preset 的原始人设描述文本（只读，标注"默认值"）
- [ ] 会话级弹窗中同样展示原始默认文本作为参考
- [ ] "预览完整提示词"按钮：触发一次 system-prompt assemble，展示合并后的完整文本
- [ ] 预览文本反映当前所有生效的覆盖（三级优先级链解析后的最终结果）
- [ ] 预览中的 `{{model}}`、`{{cwd}}` 等变量已被替换为实际值

## 评论

（评论与对话历史追加于此，新内容置于最前。）
